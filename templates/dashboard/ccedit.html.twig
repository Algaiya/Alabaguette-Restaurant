{% extends 'layout.html.twig' %}

{% block main %}
<div class="d-flex justify-content-center">
<div class="col-sm-2 ">
{{ form(formcc, {'attr': {'novalidate':'novalidate'}}) }}
</div>
</div>

{% endblock %}